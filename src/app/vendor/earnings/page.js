'use client';
import { useState, useEffect } from 'react';
import '@/app/admin/admin.css';

export default function BulkEarnings() {
    const [transactions, setTransactions] = useState([]);
    const [walletBalance, setWalletBalance] = useState('₹45,200');
    
    useEffect(() => {
        // Mock data
        setTransactions([
            { id: 'TXN-1092', type: 'Credit', amount: '₹1,500', date: '25 Aug 2026', description: 'Payout for BK-10045 (Ramesh Plumber)' },
            { id: 'TXN-1091', type: 'Credit', amount: '₹850', date: '25 Aug 2026', description: 'Payout for BK-10042 (Suresh AC)' },
            { id: 'TXN-1090', type: 'Debit', amount: '₹10,000', date: '24 Aug 2026', description: 'Withdrawal to Bank Account' }
        ]);
    }, []);

    return (
        <div className="admin-page">
            <div className="admin-header" style={{display: 'flex', justifyContent: 'space-between', alignItems: 'center'}}>
                <div>
                    <h1>Bulk Earnings & Wallet</h1>
                    <p>Consolidated financial reporting and total revenue generated by all agency technicians.</p>
                </div>
                <div style={{textAlign: 'right'}}>
                    <div style={{fontSize: '0.9rem', color: '#64748b'}}>Available Balance</div>
                    <div style={{fontSize: '1.8rem', fontWeight: 'bold', color: '#10b981'}}>{walletBalance}</div>
                    <button className="btn btn-primary" style={{marginTop: '0.5rem'}}>Request Withdrawal</button>
                </div>
            </div>

            <div className="table-container" style={{marginTop: '2rem'}}>
                <h3>Recent Transactions</h3>
                <table className="admin-table" style={{marginTop: '1rem'}}>
                    <thead>
                        <tr>
                            <th>Transaction ID</th>
                            <th>Date</th>
                            <th>Description</th>
                            <th>Type</th>
                            <th>Amount</th>
                        </tr>
                    </thead>
                    <tbody>
                        {transactions.map(txn => (
                            <tr key={txn.id}>
                                <td><strong>{txn.id}</strong></td>
                                <td>{txn.date}</td>
                                <td>{txn.description}</td>
                                <td>
                                    <span className="status-badge" style={{
                                        background: txn.type === 'Credit' ? '#dcfce7' : '#fee2e2',
                                        color: txn.type === 'Credit' ? '#166534' : '#991b1b'
                                    }}>
                                        {txn.type}
                                    </span>
                                </td>
                                <td style={{fontWeight: 'bold', color: txn.type === 'Credit' ? '#10b981' : '#ef4444'}}>
                                    {txn.type === 'Credit' ? '+' : '-'}{txn.amount}
                                </td>
                            </tr>
                        ))}
                    </tbody>
                </table>
            </div>
        </div>
    );
}
